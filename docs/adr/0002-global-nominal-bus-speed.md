# Global Nominal Bus Speed for Segment Time Estimates

Consorcio Fenix provides scheduled departure times but not trip durations or live positions, so first-version segment timestamp estimates will use one global configured nominal bus speed. This keeps sun-side advisories possible without pretending to have schedule precision, and leaves room for later per-route overrides or observed travel-time models when better data exists.
